package com.eyetwin.controller;

/**
 * UserStatisticsController — alias de UserProfileController.
 * Satisfait fx:controller="...UserStatisticsController" dans UserStatistics.fxml.
 * Toute la logique (y compris les services) est dans UserProfileController.
 */
public class UserStatisticsController extends UserProfileController {}